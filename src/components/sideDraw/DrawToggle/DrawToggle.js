import React from "react";

const drawToggle = (props) => (
  <div className="DrawerToggle" onClick={props.openSideDraw}>
    <div></div>
    <div></div>
    <div></div>
  </div>
);

export default drawToggle;
